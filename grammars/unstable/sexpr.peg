Expr   <- Atom
       / String
       / Float
       / Int
       / sp '(' sp (Expr sp)* ')' sp
Atom   <- [a-zA-Z_+?=/*]+
String <- '"' (!'"' .)* '"'
Float  <- [0-9]+ '.' [0-9]+
Int    <- [0-9]+
sp     <- [\t\n\r ]*
